---
# Default role variables for kagenti_installer
# Timeout (seconds) to wait for helm operations
helm_wait_timeout: 1200

# RHOAI defaults (overridden by values files)
rhoai:
  enabled: false
  profile: minimal
  channel: fast-3.x
  kserve_mode: raw
