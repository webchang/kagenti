# Ansible Installer (Removed)

The Ansible-based installer has been removed. Use the bash installer instead:

    scripts/kind/setup-kagenti.sh

See [docs/install.md](../../docs/install.md) for full installation instructions.

For OpenShift deployments, see [docs/ocp/openshift-install.md](../../docs/ocp/openshift-install.md).
